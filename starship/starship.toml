"$schema" = 'https://starship.rs/config-schema.json'

add_newline = true
command_timeout = 1000

# LEFT prompt — where am I + project state. Kept tight; transient info lives on
# the right (see right_format).
format = """
$directory\
$git_branch\
$git_status\
$git_state\
$git_metrics\
$nodejs$python$rust$golang$c$java$dotnet\
$jobs\
$line_break\
$status$character"""

# RIGHT prompt — ambient/transient info pushed to the right edge so it never
# crowds what you type: how long the last command took, then a clock.
# Works the same in zsh and PowerShell.
right_format = """$cmd_duration$time"""

[directory]
style = "bold cyan"
truncation_length = 3
truncate_to_repo = true
truncation_symbol = "…/"   # show that the path was shortened, e.g. …/src/app
read_only = " "

[git_branch]
symbol = " "
style = "bold purple"

[git_status]
style = "bold red"

[git_state]
style = "bold yellow"

# +added / −deleted line counts for the working tree. This is the ONE module
# that costs an extra `git diff` every prompt — if the prompt ever feels slow
# (check with `starship timings`), set disabled = true to drop it.
[git_metrics]
disabled = false
added_style = "bold green"
deleted_style = "bold red"

# The typed-at symbol. Both shells run in vi mode (zsh `bindkey -v`,
# PSReadLine `EditMode=Vi`), so the mode cue matters: insert mode is a green/red
# ❯ (green = last command ok, red = it failed); normal/command mode (after Esc)
# is a bold-yellow ❮ — the COLOR change, not just the flipped arrow, is what
# makes the mode switch visible at a glance.
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold yellow)"

# Exit code of the last command, shown only on failure, right before the ❯.
# The red ❯ already flags that something failed; this adds the actual code
# (e.g. ✘130 = Ctrl-C, ✘127 = command not found).
[status]
disabled = false
format = "[$symbol$status]($style) "
symbol = "✘"
style = "bold red"

# Background jobs (e.g. after `sleep 5 &`). Hidden when there are none.
[jobs]
symbol = " "
style = "bold blue"
number_threshold = 1

[cmd_duration]
min_time = 500
format = "[ $duration]($style)"
style = "yellow"

# Clock on the right edge. 24h hour:minute, dimmed so it stays in the background.
[time]
disabled = false
format = "[ $time]($style)"
time_format = "%H:%M"
style = "dimmed white"

[nodejs]
symbol = " "
[python]
symbol = " "
[rust]
symbol = " "
[golang]
symbol = " "
[c]
symbol = " "
[java]
symbol = " "
[dotnet]
symbol = " "
